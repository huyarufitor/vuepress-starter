# 谈一谈你理解的函数式编程？

？？？？ 

“函数式变成”是一种“编程范式”，也就是
如何编写程序的方法论。

它具有以下特性：闭包和高阶函数、惰性运算、递归、函数是“第一等公民”、只用“表达式”。

## 关键概念和特点

- 纯函数
函数的输出只依赖输入，不依赖于外部状态。相同的输入始终产生相同的输出，而且没有副作用。
- 不可变性
数据一旦创建，就不能被更改或修改。任何对数据的操作都会返回一个新的数据，而不是在原数据上进行修改。
- 无副作用
函数不应该对外部环境产生影响，例如修改全局变量、修改参数引用等。函数的操作应该是封闭的，只影响其返回值。
- 高阶函数

