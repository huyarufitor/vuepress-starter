# Mixin 是面向对象程序设计语言中的类

## 面向对象程序设计里的Mixin优点

 提供了方法的实现，有利于复杂逻辑代码的使用，避免了多继承的复杂；Mixin 类通常作为功能模块使用，在需要该功能的时候“混入”

## vue中的mixin使用

vue3已经不建议使用mixins了。取而代之的是用组合式函数（利用组合式API）；

## mixins使用缺点

（1）命名冲突
（2）不清晰的数据来源
（3）隐式的跨mixin交流
